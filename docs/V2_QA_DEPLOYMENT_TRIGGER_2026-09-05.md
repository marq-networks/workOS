# Work OS V2 QA Deployment Trigger

This documentation-only commit intentionally triggers the dedicated Vercel QA project after its Production branch was changed to `v2/functional-build`.

No application behavior, database schema, permissions, or runtime configuration is changed by this file.
