# Browser-safe values only. Never commit credentials or expose a secret key,
# service-role key, database password, or management token via VITE_*.
#
# Vercel Production must use https://zabpmtkzqetroiwbbofh.supabase.co.
# The project ref zabpmtkzqetroiwbbofh is deployment identity, not a secret.
# Vercel Preview must use a separate non-production Supabase project. Until one
# exists, leave Preview unconfigured so its deployment intentionally fails closed.
# Local development may use a developer-selected Supabase project.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
